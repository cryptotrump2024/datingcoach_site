# Committed build-time env for production deploys.
# ONLY public, client-safe values belong here — these ship in the browser
# bundle by design (Supabase Row Level Security protects all data).
# Server secrets (ANTHROPIC_API_KEY, OPENROUTER_API_KEY, STRIPE_*, the Supabase
# DB password / secret key) must NEVER go here — set those in Vercel project
# settings (Environment Variables) instead.

VITE_SUPABASE_URL=https://lravkfljouavrjilxgod.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_uWLVBDaPzsop94Tw32_0xw_wC-X_ofw
